services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gotodo
      POSTGRES_USER: gotodo
      POSTGRES_PASSWORD: gotodo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
